# MCP Server Dependencies
# Install with: pip install -r requirements.txt

# Official MCP Python SDK (includes FastMCP)
mcp>=1.0.0

# Data validation and schema generation
pydantic>=2.0.0

# Data processing
pandas>=2.0.0

# HTTP client (if connecting to real weather APIs in future)
httpx>=0.24.0

# Optional: For testing and development
pytest>=7.4.0
pytest-asyncio>=0.21.0
